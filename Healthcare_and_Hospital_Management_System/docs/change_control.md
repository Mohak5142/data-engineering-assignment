# Change Control

PDD flow: JIRA ticket -> peer review -> architecture/data governance approval -> deployment. Version every notebook, metadata schema and KPI definition. Use Git for source control and Databricks Asset Bundles for deployment.
