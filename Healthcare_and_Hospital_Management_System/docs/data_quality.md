# Data Quality

Checks: nulls, duplicate primary keys, categorical validity, dates, non-negative amounts/costs, mandatory keys, foreign-key integrity, schema validation, row-count thresholds, and quarantine of rejected records.

Score = passing checks / total configured checks. The audit log stores average DQ score per source/layer.
